
# Application environment (development, production, test, etc.)
APP_ENV=development

# Port on which the server will listen
APP_PORT=8080

# Database connection settings
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Bismillah!
DB_NAME=konten_app

# JWT secret for signing tokens (replace with a strong secret in production)
JWT_SECRET=super-secret-key

# JWT token expiry in hours
JWT_EXPIRY_HOURS=24

# Workspace restricted mode
WORKSPACE_RESTRICTED=true

# Allow new user registration
# When true: registration endpoint is accessible.
# When false: registration is disabled (returns 404).
ALLOW_REGISTRATION=false

# Cloudflare R2 Storage (S3-compatible)
# If not configured, local filesystem storage will be used
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=konten-media
R2_PUBLIC_URL=

